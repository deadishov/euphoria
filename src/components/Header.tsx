import { AppBar, Toolbar, Typography } from '@mui/material'

export const Header = () => {
    return (
        <AppBar>
            <Toolbar>
                <Typography>
                    Heey
                </Typography>
            </Toolbar>
        </AppBar>
    )
}

